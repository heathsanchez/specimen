# V172B — Issue #19 qualified admitted-baseline rerun

## Status of prior runs
V172's transfer result is scientifically useful, but its protected-suite gate is invalid as an admission gate because the workflow omitted the already-admitted V169 scoped expectation-maintenance step that is part of the qualified K7S state. V173 is therefore apparatus-confounded for the same reason and is not semantic evidence against the deletion intervention.

## Frozen question
With the full admitted pre-intervention state restored exactly — K2 + K5 + K6 + K7S plus V169's scoped three-file obsolete-diagnostic maintenance — does the unchanged V171 deletion intervention:

1. rescue the source-distinct Map-shaped and Set-shaped issue #19 fixtures;
2. preserve genuine generated-type and equality capabilities;
3. preserve the full protected build and test suite; and
4. reverse under independent ablation?

## Frozen intervention
Exactly `scripts/v171_apply_delete_default_typeparam_constraints.py`, unchanged from V171C and V172.

## Admitted baseline completion
Every arm applies `scripts/v169_remove_obsolete_redundant_match_expectations.py` after K7S. Its allowed mutation scope is frozen to exactly:
- `SpecimenTest/DeriveArbitrarySuchThat/FunctionCallsTest.lean`
- `SpecimenTest/DeriveArbitrarySuchThat/NEqGenerator.lean`
- `SpecimenTest/DeriveArbitrarySuchThat/Syntax.lean`

This is not a new verifier edit; it is the already-qualified V169 maintenance that V172 accidentally omitted.

## Arms
- A: full admitted prior state, no V171 deletion; full build/test; source-distinct transfer fixtures must still fail.
- B: same admitted state + unchanged V171 deletion; full build/test; transfer and safety fixtures.
- C: independently rebuilt admitted prior state, deletion absent; transfer failures must return.

## PASS
All frozen gates must pass. A PASS licenses retention of the issue #19 intervention as a bounded requirement-driven type-parameter binder law. Any new protected failure kills admission. Apparatus failure remains R10.