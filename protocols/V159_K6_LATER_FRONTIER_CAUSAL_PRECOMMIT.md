# V159 — K6 Later-Frontier Causal Dependence (PRECOMMIT BEFORE V158 OUTCOME)

## Purpose
Test constructor DEVELOPMENT, not merely local repair. This protocol is frozen before V158's admission outcome is inspected.

## Natural later frontier
Use the pre-existing `SpecimenTest/StrataLexprGen.lean` target. This target predates K6 and was already a natural downstream residual after admitted K5 moved the frontier into parameter-dependent typeclass synthesis. It is not authored or selected from a new protected pool to fit V158.

## Eligibility
V159 may execute scientifically only if V158 earns scoped K6 admission under all frozen V158 gates. If V158 is not admitted, V159 terminates `INELIGIBLE_K6_NOT_ADMITTED` without a constructor-development claim.

## Arms
A — K_previous = exact admitted K2 + K5.
B — K_previous + admitted V158 K6.
C — K_previous + K6-ablation = exact A rebuilt independently after B, to demonstrate reversal is attributable to K6 rather than cache/build contamination.

No Strata-specific source edits are allowed in B. The K6 implementation must be byte-identical to the admitted V158 intervention scripts/adapter stack.

## Frozen outcomes
Reachability PASS requires:
1. A fails the exact `StrataLexprGen` target at the pre-existing downstream residual (or a structurally equivalent verifier-confirmed later frontier), not R10;
2. B passes the same target;
3. C restores A's failure;
4. B passes full protected `lake build` and `lake test`;
5. no target-specific logic occurs in K6.

If A and B both pass, classify NULL/NO_REACHABILITY_MOVEMENT and compare preregistered cost only if reproducible measurements exist.
If A and B both fail, classify NEGATIVE_LATER_FRONTIER_NOT_MOVED.
If apparatus fails, R10 only.

## Cost measures
Record target wall-clock, compiler return code, generated prerequisite count if available, and full-suite build/test status. Wall-clock alone cannot establish capability.

## Claim boundary
A reachability PASS establishes one scoped constructor-development transition: K_previous → K6 → a pre-existing later natural frontier becomes reachable, with K6 ablation reversing it. It does not establish recursive/open-ended constructor invention or general self-improvement.
