# V151B1 — V151B result-plumbing apparatus repair

Frozen 2026-08-16 NZST under Rigorous Breakthrough Stack v1.1 before any V151B1 outcome.

V151B executed the frozen Lean fixture but the result reporter failed afterward because a single-quoted Python heredoc attempted to parse the literal string `${RC}`. The artifact glob also omitted `v151b.log`. This is R10 result-plumbing apparatus and carries no semantic conclusion.

V151B1 reruns the **byte-identical frozen V151B Lean fixture** (expected SHA256 `5aa931f24425d2e73170f6eeb946e691dda8dda9a3c5f716cdbb9f0d16fb68b0`). No Lean expression, check, family, dependency, or scientific gate changes.

Only apparatus changes:
- write the Lean return code to `v151b1_rc.txt` and read that file from Python;
- upload `v151b1_*` including the raw Lean log and result JSON.

Interpretation remains exactly V151B's preregistration. Any fixture hash mismatch is R10.