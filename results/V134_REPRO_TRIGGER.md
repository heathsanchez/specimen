# V134 reproduction trigger

This file carries no semantic change and authorizes no K6 repair.

Its only purpose is to open a pull request after the frozen V134 workflow acquired a `pull_request` trigger on the base branch, so the already-frozen V134 specialization-before-instance-synthesis discriminator can execute and expose its result before any next mechanism is designed.
