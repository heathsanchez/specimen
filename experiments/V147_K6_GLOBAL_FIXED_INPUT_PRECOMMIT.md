# V147 K6 global-fixed-input candidate — frozen precommit

Controller: Rigorous Breakthrough Stack v1.1.

Do not execute this candidate while a lawful old-closure separator remains unresolved. V145b is the final apparatus repair for the let-bound old-language rival. If V145b demonstrates old closure, V147 must be abandoned without intervention.

## Residual if construction becomes licensed
Under exact admitted K2+K5, direct global fixed indices reproduce `NON_FVAR_APPLICABILITY`; generic parameterization reaches a separate dependent-instance residual; true instance-binder, relation-instance, and auto/fallback routing alternatives did not close it.

## Minimal candidate
`K6_GLOBAL_FIXED_INPUT`: support a non-output relation argument that is syntactically a **global constant** by preserving that global constant as fixed through standalone/fallback schedule derivation and code emission. It must not be converted into a fresh universally quantified parameter. No fixture/type/relation/constant name may be special-cased.

This candidate deliberately does NOT claim support for arbitrary closed expressions, literals, reducible abbreviations, or the auto-derive `SpecKey` path.

## Frozen causal arms
Using `specimen.autoDeriveDeps false` in every arm:
- B: exact K2+K5, direct global fixed-index target, no K6.
- I: exact K2+K5 + K6_GLOBAL_FIXED_INPUT.
- A: same intervention harness with the K6 patch omitted (targeted mechanism ablation; must reproduce B).
- G: generic symbolic parameter target under I, preserving the distinct missing-dependent-instance residual.

## PASS gates
A local causal K6 PASS requires:
1. B = NON_FVAR_APPLICABILITY.
2. I derives and `#synth`s the original predicate at the fixed global constant.
3. A restores NON_FVAR_APPLICABILITY.
4. G remains SYMBOLIC_INSTANCE; K6 must not manufacture a missing generic instance.
5. Full protected build/test under I are green.
6. Static patch audit finds no V147 fixture identifiers, target relation/type names, or target constant names.

## Promotion boundary
Even a local PASS earns only a scoped candidate mechanism. K6 is not admitted until source-distinct held-out transfer passes with targeted ablation. Constructor development additionally requires a later constructor/capability frontier to become newly reachable or materially cheaper because K6 is retained, with K6 ablation reversing that later advantage.